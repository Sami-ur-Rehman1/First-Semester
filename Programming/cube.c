#include<stdio.h>
void main ()
{
    float length;
    printf("enter the length of one side of square");
    scanf("%f",&length);
    float square=length*length;
    printf("square%f",square);
}
